# Veritabani (default SQLite — lokal demo)
DATABASE_URL=sqlite:///data/gymtracker.db

# Neon/Postgres'e gecmek istersen (yorum satirini ac, ustteki satiri kapat):
# DATABASE_URL=postgresql+psycopg://USER:PASS@HOST/db?sslmode=require

# Pose model yollari (repo-relative)
MEDIAPIPE_MODEL_PATH=models/pose_landmarker_full.task
YOLO_MODEL_PATH=yolo26m-pose.pt

# Log seviyesi
LOG_LEVEL=INFO
